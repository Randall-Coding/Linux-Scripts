BEGIN {
        FS= "<head>"
        while (getline) {
          if ($0 ~ /<\head>/) {
            print "\n\n" FS " " $0 $1 $2
            break
          }
        }
      }
{
 FS ="<\/head>"
 while (getline) {
   if ($0 ~ /<\/FS/) {print $1 FS; break}
   else {print}
 }
}
